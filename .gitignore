# dependencies
node_modules

# build output
dist
dist-ssr

# test coverage
coverage

# local runtime config — never commit real values (keep config.yaml.example)
config/config.yaml

# logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.local
